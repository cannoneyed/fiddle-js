import { style } from 'typestyle'
import * as sequencer from 'styles/sequencer'

export const toolbarContainer = style({
  $debugName: 'toolbarContainer',
  position: 'absolute',
  top: 0,
  height: sequencer.toolbarHeight,
})
