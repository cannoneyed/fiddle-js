import styled from 'styled-components'
import * as sequencer from 'styles/sequencer'

export const ToolbarWrapper = styled.div`
  position: absolute;
  top: 0px;
  height: ${sequencer.toolbarHeight};
`
